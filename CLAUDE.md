# Note++ — Project Context

> Claude reads this file at the start of every session. Keep it up to date.

## What This Is

**Note++** — a developer-focused desktop text editor built as an Electron + Monaco clone of Notepad++.  
Located at `D:\NewNotepad`. GitHub: https://github.com/YogeshPraj/Note-

**Target user:** software developers (not generic notepad users).  
**Key differentiators over plain Notepad++:** IntelliSense, integrated terminal, live HTML/Markdown preview with Mermaid diagrams, cloud session sync, Monaco editor engine.

---

## Tech Stack

| Layer | Tech |
|---|---|
| Runtime | Electron 32 (Node 20.18, Chromium 128) |
| Editor | Monaco Editor 0.45.0 |
| Markdown | marked v18.0.3 |
| Diagrams | Mermaid v11.14.0 |
| Whiteboard | Excalidraw 0.18 + React 18 (bundled via esbuild into iframe) |
| Terminal | xterm 5.3 + xterm-addon-fit + node-pty 1.1 |
| Bundler (iframe only) | esbuild 0.24 |
| Security | `contextIsolation: true`, `nodeIntegration: false` |
| Node bridge | `src/preload.js` → `window.electronAPI` |
| Platform | Windows, macOS, Linux (cross-platform builds via electron-builder matrix CI) |
| Auto-update | electron-updater + GitHub provider (latest.yml / latest-mac.yml / latest-linux.yml on every release) |

---

## Project Structure

Current as of **v1.7.2** (renderer.js ~7,100 lines; main.js ~1,500; style.css ~2,200).

```
D:\NewNotepad\
├── package.json              ← "main": "src/main.js", version 1.7.2
├── package-lock.json
├── launch.bat                ← double-click to run (npx electron .)
├── build-whiteboard.js       ← esbuild script: bundles Excalidraw React app + copies fonts
├── .gitignore
├── CLAUDE.md                 ← this file
├── README.md                 ← GitHub home page
├── features/                 ← per-feature design specs (moved here in Session 9)
│   ├── AGENT.md, DIFF.md, ENCRYPTION.md, GIT.md, LSP.md, ROADMAP.md
├── tools/                    ← scratch / legacy artefacts not in shipped app
├── .github/workflows/
│   └── release.yml           ← 3-OS matrix on tag push; electron-builder sole uploader
└── src/
    ├── main.js                  ← Electron main process (IPC, file dialogs, menus)
    ├── preload.js               ← IPC bridge (window.electronAPI)
    ├── index.html               ← Renderer entry — script load order is delicate (see below)
    ├── renderer.js              ← All UI logic (~7,100 lines; module split started Session 13)
    ├── style.css                ← All styles
    ├── monaco-worker.js         ← Monaco web worker helper
    ├── crypto.js                ← AES-GCM / PBKDF2 / HKDF / gzip (encrypted pad)
    ├── git-service.js           ← Git CLI wrapper (main process)
    ├── lsp-service.js           ← Main-process LSP subprocess manager + JSON-RPC
    ├── lsp-client.js            ← Renderer-side bridge: Monaco providers ↔ LSP requests
    ├── drawio-service.js        ← On-demand download + extract for draw.io bundle
    ├── drawio.html              ← draw.io iframe shell (postMessage broker)
    ├── drawio-templates.js      ← 4 starter template XMLs (extracted Session 13)
    ├── drawio-helpers.js        ← createDrawioTab + download modal + check-updates
    ├── whiteboard.html          ← Excalidraw iframe shell
    ├── whiteboard-app.jsx       ← React + Excalidraw entry (source)
    ├── whiteboard.bundle.{js,css}  ← generated by esbuild (gitignored)
    ├── whiteboard-helpers.js    ← nextWbTabNumber / sendToWhiteboard / createWhiteboardTab
    ├── whiteboard-assets/       ← generated: fonts referenced by bundle.css (gitignored)
    ├── excalidraw-fonts/        ← generated: Excalidraw runtime fonts (gitignored)
    ├── mermaid-live-view.html   ← Mermaid Live Editor overlay
    ├── lazy-loaders.js          ← ensureMermaid / ensureXterm / ensureDiff (UMD AMD-trap guards)
    ├── updater.html             ← Auto-update progress + success window
    ├── updater-preload.js       ← Minimal preload for the updater window
    ├── assets/                  ← 17 SVG toolbar icons
    └── games/                   ← in-app HTML games (Dev Arcade)
```

---

## Architecture Notes

### IPC Pattern
All Node/filesystem access goes through `preload.js`. The renderer calls `window.electronAPI.readFile(path)`, etc. Never use `require()` directly in renderer code.

### Monaco AMD Loader — Critical Loading Order
Monaco's `vs/loader.js` installs a global `define()`. **Every UMD script that exposes its API via `window.X` must either load before `vs/loader.js`, OR use `lazy-loaders.js`'s `_loadUmdScript()` which briefly nulls `window.define` for the duration of the load.** The classic failure mode: UMD scripts loaded after vs/loader.js register as anonymous AMD modules and never set their global.

Current script load order in `index.html` (as of Session 13 lazy-loading work):

1. `marked.umd.js`                    ← MUST be first (eager load)
2. `vs/loader.js`                     ← installs global define()
3. `lazy-loaders.js`                  ← ensureMermaid / ensureXterm / ensureDiff helpers
4. `drawio-templates.js`              ← pure data + 1 shim
5. `crypto.js`                        ← Web Crypto wrappers (encrypted pad)
6. `lsp-client.js`                    ← LSP bridge
7. `renderer.js`                      ← main UI logic
8. `whiteboard-helpers.js`            ← createWhiteboardTab + nextWbTabNumber
9. `drawio-helpers.js`                ← createDrawioTab + download modal

**Lazy-loaded on first use** (NOT in index.html):
- `mermaid.min.js` (~3 MB) — first preview render needing a diagram
- `xterm.js` + `xterm-addon-fit.js` (~270 KB) — first integrated-terminal open
- `diff/dist/diff.min.js` (~50 KB) — first inline-git-diff-gutter call

Each lazy-load goes through `_loadUmdScript()` which nulls `window.define` during the load so the UMD scripts fall back to their global-attach branch instead of being intercepted by Monaco's AMD loader.

### Monaco Worker URLs
Since `index.html` lives in `src/`, all `node_modules` paths use `../node_modules/`. The Monaco worker inline script in `index.html` uses `${base}../node_modules/monaco-editor/min/`.

In `renderer.js`, the require config uses `'../node_modules/monaco-editor/min/vs'`.

### Preview Panel
- Toggled with `Ctrl+Shift+V` or the 👁 toolbar button
- Only activates for `html` and `markdown` language modes
- Split-pane: editor left, preview right (resizable via drag handle)
- 400ms debounce on keystroke before re-render
- **Markdown preview**: `marked.parse()` with custom `renderer.code` override for Mermaid fences; result injected into `#preview-md-content`
- **Mermaid**: bare Mermaid files (no code fences, starts with `graph`/`flowchart`/etc.) are detected by `isBareRawMermaid()` and rendered directly
- **HTML preview**: rendered in sandboxed `<iframe srcdoc="...">` with mermaid script injected into `<head>`
- **Mermaid Live Editor** (`.mmd` / `.mermaid`): dedicated split-pane editor — Monaco left, live diagram right. Auto-opens preview. Toolbar: templates, SVG/PNG export, zoom controls (Ctrl+scroll too). Error box shows parse failures while preserving last valid diagram. Uses `mermaid.render()` API for direct SVG injection.

### Mermaid (v11) API
```javascript
// For Markdown/HTML preview (run on all .mermaid divs):
mermaid.initialize({ startOnLoad: false, theme: 'default'|'dark', securityLevel: 'loose' });
mermaid.run({ nodes: Array.from(container.querySelectorAll('.mermaid')) });

// For Mermaid Live Editor (direct SVG render):
const { svg, bindFunctions } = await mermaid.render(uniqueId, diagramText);
container.innerHTML = svg;
if (bindFunctions) bindFunctions(container);
```

---

## Features Implemented

**Editing**
- [x] Multi-tab Monaco editor; 50+ language syntax; Monaco's built-in IntelliSense
- [x] **LSP integration** — Pyright for Python out of the box; status-bar pill with click-to-install when missing; registry-driven so adding new languages is one entry in `lsp-service.js`
- [x] **Inline git diff gutter** — green/blue/red bars vs `HEAD`, jsdiff-powered
- [x] Find / Replace / Mark / Find-in-Files with Notepad++-style results panel + live gutter+minimap markers
- [x] Command palette (`Ctrl+Shift+P`), Quick Open (`Ctrl+P`)
- [x] Bookmarks, breadcrumbs, minimap, word wrap, indent guides, render whitespace
- [x] **Persistent UI toggles** — Word Wrap / Dark Mode / Zoom / Minimap / Whitespace / Indent / Toolbar / Statusbar / Tabbar survive restarts (`settings.json` under `ui.*`)
- [x] Recent Files menu (last 15, deduped, stale-pruned)
- [x] **External-change watcher** — clean tabs auto-reload; dirty tabs ask first
- [x] **Session dirty-flag persistence** — unsaved tabs come back red-dotted after restart

**Diagrams & previews**
- [x] Live preview for HTML + Markdown (`Ctrl+Shift+V`) with zoom + maximise
- [x] Mermaid Live Editor for `.mmd` / `.mermaid` (lazy-loaded since Session 13)
- [x] Mermaid blocks rendered in MD preview + HTML preview
- [x] **Whiteboard** — Excalidraw 0.18, save lifecycle matches text tabs (red-dot dirty, Save As prompt; no silent AppData auto-save since v1.5.1)
- [x] **draw.io structured diagrams** — on-demand bundle download (~40 MB) into userData, persists across upgrades; 4 starter templates (Flowchart / Sequence / Class / ER); served via custom `drawio://app/...` privileged protocol

**Source control + AI**
- [x] Source Control side panel (`Ctrl+Shift+G`) — staging, commit, branches, push/pull/sync, file-tree decorations
- [x] AI Assistant (Ollama) with multi-turn chat + Agent mode + quick-action chips
- [x] `AGENTS.md` + `.notepp/memory.md` auto-injected into AI prompts
- [x] Multi-model picker (chat vs agent)

**Encryption & privacy**
- [x] Per-file AES-256-GCM encryption with recovery key
- [x] Profile + DEK wrapped in `%AppData%\notepp\encryption\profile.json`

**Compare**
- [x] File compare (Monaco DiffEditor)
- [x] Folder compare (dir-compare) with Diff.Net-style tree
- [x] Right-click tab → Select for Compare / Compare with selected

**Productivity**
- [x] Integrated terminal (xterm + true PTY via node-pty; lazy-loaded since Session 13)
- [x] File tree sidebar with chunked rendering for large folders
- [x] Run file (F5) — types `cd "<dir>"; <runner> "<file>"` into the live PTY
- [x] Cloud session sync detection (Google Drive / OneDrive / Dropbox)
- [x] Auto-save session (`.json` autosave dir) + auto-backup with version retention

**Distribution & auto-update**
- [x] Cross-platform builds — Windows NSIS + .zip portable; macOS x64 + arm64 DMG; Linux AppImage + .deb (3-OS GitHub Actions matrix)
- [x] **Auto-update** via electron-updater — toolbar pill shows `New update available → Downloading X% → Click to update`; click hides main window and opens a dedicated progress window with success animation

**Tools menu**
- [x] Regex tester · Base64 encode/decode · MD5/SHA-256 from selection
- [x] Pretty-print + minify JSON/XML
- [x] Diagram (draw.io) submenu: New Diagram · From Template ▶ · Check for updates

---

## Key renderer.js Functions

| Function | Purpose |
|---|---|
| `setupPreview()` | Wires close/refresh buttons and col-resize drag |
| `togglePreview()` | Opens if previewable lang, closes if open |
| `updatePreview()` | Routes to `renderMarkdownPreview` or `renderHtmlPreview` |
| `renderMarkdownPreview(content, container)` | Parses MD with marked, runs Mermaid |
| `renderBareMarkdown(content, container)` | Wraps raw Mermaid content in `.mermaid` div |
| `runMermaidInContainer(container)` | Calls `mermaid.run()` on all `.mermaid` nodes |
| `renderHtmlPreview(content, frame, tab)` | Injects mermaid script, sets `iframe.srcdoc` |
| `isBareRawMermaid(text)` | Returns true if entire file is raw Mermaid syntax |
| `schedulePreviewUpdate()` | 400ms debounced call to `updatePreview()` |
| `activateTab(id)` | Switches active tab, updates editor model + preview |
| `createTab(opts)` | Creates new tab object + DOM tab element |
| `renderMermaidPreview(content)` | Renders Mermaid SVG into `#mermaid-diagram` via `mermaid.render()` |
| `renderMmdLiveView(content)` | Renders Mermaid SVG into `#mmd-live-view` overlay (fullscreen) |
| `toggleMmdLiveView()` | Toggles between local zoom-panel and fullscreen `#mmd-live-view` overlay |
| `createWhiteboardTab(filePath, content)` | Creates whiteboard tab; auto-names `whiteboard-N.json`; kicks off `initWbFile` if new |
| `initWbFile(tab)` | Async: resolves userData path, creates `Whiteboards/` dir + initial JSON file, sets `tab.filePath` |
| `scheduleWbFileSave(tab)` | Per-tab debounced (1.5 s) write of `tab.content` to `tab.filePath` |
| `nextWbTabNumber()` | Returns lowest unused N for `whiteboard-N.json` naming |

---

## Session History

### Session 1 (origin: 69d8d00e)
- Scaffolded entire Note++ application from scratch
- Electron main process, preload bridge, Monaco editor setup
- Multi-tab system, file open/save, syntax highlighting
- Terminal integration (xterm + PowerShell)
- Find/Replace panel, command palette, quick open
- File tree sidebar, status bar, toolbar
- Dark mode, preferences dialog, cloud sync

### Session 2 (continued from Session 1)
- Added HTML + Markdown live preview panel (`Ctrl+Shift+V`)
- Resizable split-pane layout (CSS flex)
- Mermaid diagram support in Markdown preview (code fences)
- **Bug fix**: Preview was empty — root cause was Monaco AMD loader intercepting marked's UMD registration. Fixed by moving `marked.umd.js` script tag before `vs/loader.js`.
- Added `isBareRawMermaid()` detection for raw Mermaid files (no code fences)
- Moved all source files to `src/` subdirectory
- Updated all `node_modules/` paths to `../node_modules/` in `src/`
- Initialized git repo, pushed initial commit to https://github.com/YogeshPraj/Note-

### Session 3 (continued from Session 2)
- **"Live" button fix**: Replaced `<webview>` pointing to `mermaid.live` (which rendered tiny/dark) with a local `<div id="mmd-live-view">` overlay rendered by the in-process mermaid library
- `renderMmdLiveView()` renders SVG via `mermaid.render()` and overlays the full preview pane via `position:absolute;inset:0;z-index:5`
- **Bug fix**: Live view was blank — root cause was stripping SVG `width`/`height` attributes then applying CSS `width:auto!important`, collapsing SVG to 0px. Fix: keep SVG attributes, override with `style="width:100%;height:auto"` inline
- **Bug fix**: Stale V8 code cache caused old code to run after edits — cleared `AppData/Roaming/notepp/Code Cache/` to force recompile
- Removed `openDevTools({ mode: 'detach' })` from `main.js`
- Removed all debug instrumentation (`document.title='DBG...'`, `showToast('DBG...')`) from preview functions

### Session 4 (continued from Session 3)
- **Whiteboard feature**: Full canvas whiteboard implemented as a lazy-loaded iframe tab (`whiteboard.html` + `whiteboard.js`)
- **Root cause of JS not running**: Electron disk cache (`AppData/notepp/Cache/`) was serving the OLD `whiteboard.html` containing a `drawEl` variable name conflict (`let drawEl = null` + `function drawEl(el)`). Fixed by: (a) renaming in-progress element to `drawEl_active`, (b) clearing all Chromium caches on dev startup in `main.js`
- **Cache clearing**: `main.js` now clears `Code Cache`, `Cache`, `DawnCache`, `GPUCache` on every non-packaged run
- **F12 DevTools**: Added `before-input-event` handler so F12 toggles DevTools in dev mode; existing `? > Developer Tools` menu item also works
- **`showFloatingMenu` overflow fix**: Menu was positioned with `top: window.innerHeight - 30`, going off-screen. Fixed with overflow detection — flips upward when near bottom edge; also fixes horizontal overflow. Status bar language picker now correctly opens above the bar.
- **Language picker Y position**: Changed from `window.innerHeight - 30` to `statusLang.getBoundingClientRect().bottom` for accurate anchoring
- **Whiteboard iframe CSP**: `whiteboard.html` has its own permissive CSP meta tag (`default-src * 'unsafe-inline' 'unsafe-eval' data: blob:`) so it runs independently of the parent page's CSP
- **`webSecurity: false`**: Was temporarily added for diagnosis; removed once confirmed the cache was the real issue
- **Confirmed working**: Drawing (rectangle, diamond, ellipse, arrow, line, pencil, text), color palette (12 swatches), fill modes (none/solid/hatch), stroke widths, undo/redo, export PNG, clear all, zoom/pan

### Session 5 (continued from Session 4)
- **Whiteboard JSON auto-save**: New whiteboard tabs auto-named `whiteboard-N.json` (lowest unused N, same gap-fill convention as editor tabs `new N`). On creation, `initWbFile()` immediately creates `%AppData%\notepp\Whiteboards\whiteboard-N.json` with a blank canvas JSON. Every committed drawing action triggers `scheduleWbFileSave(tab)` — a **per-tab** 1.5 s debounced write (stored in `wbFileSaveTimers: Map<tabId, timer>`) so multiple open whiteboard tabs each have independent timers. Closing a tab flushes its timer synchronously; no "Save?" dialog since the file is always current.
- **`write-file` IPC updated**: `main.js` now runs `mkdirSync({ recursive: true })` on the parent directory before writing, so `Whiteboards/` is auto-created without a separate IPC call.
- **`__wb__` format signature**: `whiteboard.js` embeds `{ __wb__: true, version: 1, ... }` in every `wb-state` and `wb-get-data` postMessage. Opening a `.json` file whose parsed content has `__wb__ === true` routes it directly to a whiteboard tab instead of Monaco.
- **`wb` pill badge in tab**: A small "wb" pill (blue, dark-mode aware) is inserted between the 🖼 icon and filename in every whiteboard tab so `.json` whiteboard files are visually distinct from regular JSON tabs. Style: `.tab-wb-badge` in `style.css`.
- **Save As dialog**: Primary filter is now `Whiteboard JSON (.json)`; `.whiteboard` kept as legacy option.
- **Session restore**: Whiteboard tabs with a `filePath` now always re-read from disk (file is authoritative since auto-save keeps it current).
- **Known issue (not yet fixed)**: Whiteboard text toolbar behaviour needs investigation — reported at the start of the next session.

---

### Session 6 (continued from Session 5)
- **Audit pass on `Known Issues` list**: most entries were stale.
  - `node-pty` (#1) and `electron-builder` (#2) were already in `package.json` and fully wired in `main.js` — confirmed working (PTY uses `pty.spawn` + `proc.resize`; `npx electron-builder --version` → 26.8.1).
  - `pref-new-doc` / `pref-backup` (#5) pages were fully implemented end-to-end with persistence to `settings.json`, an auto-backup timer, and an in-process `runBackup()` helper. Fixed one real latent bug in the backup tab: the "Browse…" button passed the raw dialog result through `res.canceled / res.filePaths`, but `openFolderPicker` in `main.js` already unwraps to a string-or-null. Result: clicking Browse silently did nothing. Now it just assigns the returned string.
- **Whiteboard text toolbar bugs (#6) fixed in `whiteboard.js`** — see "Known Issues / Recently resolved" for the three sub-fixes (focus-stealing palette clicks, redundant pushHist while editing, swatch live-recolour during edit).

---

### Session 7 (continued from Session 6) — **Excalidraw migration**

Replaced the entire custom canvas whiteboard with **Excalidraw 0.18** running inside the same iframe boundary. The legacy `whiteboard.js` (~700 LOC of custom rendering, hit-testing, selection, text-tool, palette, etc.) is **deleted**.

**What changed:**
- Added deps: `react@^18`, `react-dom@^18`, `@excalidraw/excalidraw@^0.18`, `esbuild@^0.24` (devDep).
- New file `src/whiteboard-app.jsx` — React entry that mounts `<Excalidraw />`, maintains the unchanged postMessage protocol (`wb-load` / `wb-state` / `wb-theme` / `wb-get-data` / `wb-save-request` / `wb-ready`), and converts legacy v1 files via `legacyV1ToV2()` on load. Debounces `onChange` by 350 ms and dedups identical state snapshots to avoid write storms.
- New file `build-whiteboard.js` — esbuild pipeline: copies Excalidraw fonts to `src/excalidraw-fonts/`, bundles JSX → `src/whiteboard.bundle.{js,css}` (IIFE, target Chrome 120, minified, `production` export condition). 7.6 MB bundle + 141 KB CSS. Build takes ~400 ms.
- npm scripts: `start` and all `build:*` scripts now chain `npm run build:wb` first. `postinstall` runs the bundle so `npm install` is self-bootstrapping.
- `src/whiteboard.html` rewritten as a tiny shell that mounts `#root` and loads the bundle.
- `src/renderer.js`:
  - `initWbFile()` now writes a v2 envelope (`{__wb__:true, version:2, source:'excalidraw', elements:[], appState:{}, files:{}}`) instead of v1.
  - `openFile()` routing extended to `.excalidraw` files and to raw Excalidraw exports (`type === 'excalidraw'`).
  - Save As filter list adds `.excalidraw`.
- `package.json`:
  - New `excalidraw` file-association registered with NSIS installer (Open With… → Note++).
  - `electron-builder` `files` glob covers `build-whiteboard.js` and (via `src/**/*`) the generated bundle, CSS, fonts. `asarUnpack` for `node-pty` so its native binary survives packaging.
- `.gitignore` excludes the generated bundle, asset folder, and self-hosted font folder (regenerated on every `npm install` / `npm start`).

**Backward compatibility:**
- Existing `whiteboard-N.json` files (v1 format) still open. `parseIncoming()` detects the version, runs `legacyV1ToV2()`, and feeds the converted scene to Excalidraw via `updateScene({ commitToHistory: false })`. The file stays v1 on disk until the user edits it, then auto-upgrades to v2 on the next debounced save.
- Old `.whiteboard` extension still routes to the whiteboard tab.
- Mapping covered: rectangle / ellipse / diamond / arrow / line / pencil (→ `freedraw`) / text (→ Virgil font).

**Build / startup integrity verified:**
- `npm run build:wb` produces clean output every run.
- All JS/JSON files syntax-validate.
- No errors reported by VS Code language services on `whiteboard-app.jsx`, `renderer.js`, `build-whiteboard.js`.

**Net code change:** ~700 lines of custom canvas code deleted, replaced by ~280 lines of bridge code in `whiteboard-app.jsx` and an 80-line build script. Net features gained: image embedding, libraries, arrow-binding, labelled arrows, frames, laser pointer, proper text editing, rotation handles, multi-select transforms, snap-to-grid, i18n, accessibility, industry-standard `.excalidraw` interchange format.

---

### Sessions 8–13 (compressed — see git log for per-commit detail)

**Session 8 — cross-platform builds (v1.6.x)**
3-OS matrix CI; node-pty N-API prebuilds (no rebuild needed across Win/Mac/Linux); icon fix (electron-builder rejects .ico for mac/linux → extracted PNG at 1024×1024 via ffmpeg); split workflow into build + finalise jobs.

**Session 9 — repo reorganisation + auto-update + LSP + dynamic About + Pyright click-to-install**
- `features/` folder (moved AGENT.md, DIFF.md, ENCRYPTION.md, GIT.md, LSP.md, ROADMAP.md out of root)
- `tools/` folder (scratch test files moved out of root)
- LSP integration: `lsp-service.js` (main) + `lsp-client.js` (renderer); Pyright for Python; status pill goes `⬇ Install pyright` (clickable) when missing → spawns `npm install -g pyright`
- Inline git diff gutter via jsdiff
- Persistent UI toggles wired through `saveSetting('ui.*')`
- Session restore now preserves the `dirty` flag (was always reset to false)
- About dialog now reads version dynamically from `app.getVersion()` (no more hardcoded `1.0.0`)
- Auto-update: electron-updater + GitHub provider + toolbar pill UI + dedicated progress window (`updater.html` + `updater-preload.js`). Pill states: available (blue) → downloading X% (amber) → click-to-update (green)

**Session 10 — draw.io on-demand integration**
- `drawio-service.js` (main) — pinned to v30.0.2, downloads `draw.war` (~40 MB) into `userData/drawio-bundle/` on first use; persists across upgrades because electron-updater leaves userData untouched
- Custom `drawio://app/...` privileged protocol (NOT `file://`) with path-traversal guard
- `drawio.html` iframe shell speaking the embed `proto=json` postMessage protocol
- Tools menu restructured into `Diagram (draw.io)` ▶ submenu: New Diagram · From Template (Flowchart/Sequence/Class/ER) · Check for updates
- Tab type `'drawio'` with amber `dw` pill badge
- Save lifecycle matches text tabs (red-dot dirty on edit, Save As for new)
- Dark mode sync via `dw-theme` configure message
- Bug fixes shipped iteratively: virtual-host URL bug (`Note++` filename URL-encoded as `Note.-`), tab-switch was a no-op (sendToDrawio queried wrong iframe id), autosave never enabled (drawio defaults autosave:0), load-echo guard via loadGen + loadSuppress

**Session 11 — performance pass (v1.7.0)**
- Lazy-load Mermaid (~3 MB), xterm + xterm-addon-fit (~270 KB), diff (~50 KB) — all via `lazy-loaders.js` with AMD-trap guard (briefly null `window.define` during dynamic script load)
- Sourcemaps + tests + docs stripped from production installer (~-20 MB)
- Non-critical init deferred to `requestIdleCallback` (git, encryption, source-control panel setup)
- Git fetch polling: 3 min → 5 min, skip when window hidden, reentrancy guard
- CSS containment on 9 heavy panels
- File-tree chunked rendering (>200 entries → batches of 100 via rIC)
- V8 code-cache `bypassHeatCheck` + disable renderer/timer backgrounding

**Session 12 — Electron 28 → 32 upgrade**
- electron-builder 32.3.3; Node 18.18 → 20.18; Chromium 120 → 128
- node-pty needed no rebuild (N-API ABI-stable across Node 18/20)
- All deps + builds healthy on Windows; CI validates Mac/Linux on tag push

**Session 13 — renderer.js module split (Phase 1+2) + clean release**
- Extracted 4 modules from the 7,100-line renderer.js as classic scripts sharing the global scope (proven pattern; no ES-module conversion yet):
  - `lazy-loaders.js` — `_loadUmdScript`, `ensureMermaid`, `ensureXterm`, `ensureDiff`
  - `drawio-templates.js` — `DRAWIO_TEMPLATES` data + `createDrawioTabFromTemplate` shim
  - `whiteboard-helpers.js` — `nextWbTabNumber`, `sendToWhiteboard`, `createWhiteboardTab`
  - `drawio-helpers.js` — `nextDrawioTabNumber`, `sendToDrawio`, `showDrawioDownloadModal`, `ensureDrawioInstalled`, `checkDrawioForUpdates`, `createDrawioTab`
- Renderer split deferred for later phases (high-risk): Source Control panel (~600 lines), AI Assistant (~700), Find / Replace / Mark (~600), Encryption (~500), tab management (~500)
- v1.7.1 release asset cleanup — consolidated naming (`notepp-<os>-<arch>.<ext>`), removed softprops file-upload (was duplicating electron-builder's uploads), version-less filenames so `releases/latest/download/<file>` redirect always works
- v1.7.2 = current development version
- SignPath signing job experiment removed — no SignPath subscription, no Apple cert, no GPG key. Workflow back to direct build+publish.

**Session 13 — REVERTED EXPERIMENT: inline AI completion (FIM)**
Spiked a Cursor-Tab-style inline AI completion feature using Ollama FIM models (Qwen-Coder, DeepSeek-Coder, StarCoder, CodeLlama, CodeGemma). Four commits implemented FIM backend + Monaco provider + trigger heuristics + status pill. **Reverted before push** because:
1. FIM models are code-only — work poorly on prose / Markdown / mixed content
2. Note++ is positioned for both writers and developers; a code-only feature wasn't broadly enough applicable

For future revival, see git reflog around the SignPath revert commit (`089f295`) for the four feat(ai) commits that existed locally. Pattern was sound; only the "code-only" trade-off killed it.

---

## How to Run

```bash
cd D:\NewNotepad
npm start          # or double-click launch.bat
```

## How to Build

```bash
# Build for the current OS (auto-detects)
npm run build

# Or target a specific platform explicitly
npm run build:win        # Windows: NSIS installer (.exe)
npm run build:msi        # Windows: MSI installer
npm run build:mac        # macOS: .dmg + .zip (x64 + arm64)
npm run build:linux      # Linux: .AppImage + .deb (x64)
```

**Cross-platform note:** electron-builder must be run on the target OS for native binaries (node-pty, native deps) to compile correctly. You can't build Mac artifacts on a Windows machine and vice-versa. The GitHub Actions workflow (`.github/workflows/release.yml`) handles this automatically via a 3-OS matrix on every tag push — each runner builds + publishes its own platform's artifacts to the same GitHub release.

**Mac caveat:** builds are currently unsigned (no Apple Developer cert configured in CI). First-launch on macOS requires right-click → Open to bypass Gatekeeper.

**Linux caveat:** AppImage builds need `libfuse2` on the build machine (CI installs it automatically). End-users on modern Ubuntu (24.04+) may need `sudo apt install libfuse2t64` to launch the AppImage.

---

## Known Issues / Future Work

**Open**
- [ ] macOS code-signing (currently unsigned; Gatekeeper right-click→Open on first launch)
- [ ] No Snap or Flatpak Linux targets (AppImage + .deb only)
- [ ] Renderer.js still ~6,500 lines after Phase-1+2 split — Source Control / AI / Find / Encryption blocks not yet extracted
- [ ] drawio download leak: if extract fails partway, temp `.war` (~40 MB) + extract dir are left in `os.tmpdir()`. The cleanup is outside a `finally{}` — see drawio-service.js download()
- [ ] Auto-updater zip files (`notepp-mac-x64.zip` etc.) visible on releases page even though they're internal — GitHub can't hide them
- [ ] Inline AI completion deferred — see Session 13 REVERTED note above. Would need either a prose-capable model + different prompt strategy, OR explicit opt-in for code-only mode.

**Recently resolved** (truncated to the last few sessions; older entries in git log)
- ~~v1.7.0 release had 27+ asset files~~ → Session 13 cleanup: electron-builder sole uploader, `notepp-<os>-<arch>` naming
- ~~Tab-switch silently swapped no diagram~~ → Session 10: `sendToDrawio` queried wrong iframe id (one-char fix)
- ~~drawio iframe Loading... never resolved~~ → Session 10: empty xml requires a valid `<mxfile>` envelope, not `''`
- ~~Whiteboard auto-saved silently to AppData~~ → Session 9: save lifecycle now matches text tabs
- ~~Mermaid loaded eagerly at startup (~3 MB)~~ → Session 11: lazy-loaded on first preview
- ~~All UMD scripts loaded after vs/loader.js silently broke (xterm Terminal undefined)~~ → v1.4.1: reorder; current state uses `lazy-loaders.js` AMD-trap guard
- ~~Source-control + LSP + encryption init blocked first paint~~ → Session 11: deferred via requestIdleCallback
- ~~Bottom-right "Whiteboard" click did nothing on whiteboard tabs~~ → Session 13: picker now context-aware (whiteboard / drawio / game tabs show only 3 new-tab actions, no language list)

### Recently resolved (Session 8 — cross-platform builds)
- ~~Windows-only builds~~ → electron-builder now produces `.exe` (Windows), `.dmg`/`.zip` (macOS x64+arm64), `.AppImage`/`.deb` (Linux x64). CI is a 3-OS matrix; tag push triggers parallel builds on `windows-latest`, `macos-latest`, `ubuntu-latest`. All artifacts plus `latest*.yml` updater manifests land on the same GitHub release.
- ~~Hardcoded `\\` separators in renderer paths~~ → switched to `/` and `[\\/]` regex (works on every OS).
- ~~`powershell.exe` hardcoded as terminal default~~ → `defaultShellForPlatform()` picks `powershell.exe` on Windows, `/bin/zsh` on macOS, `/bin/bash` on Linux.
- ~~F5 Run File used PowerShell-specific `;` operator~~ → uses `&&` on macOS/Linux so bash/zsh parse cleanly.

### Recently resolved (Session 6)
- ~~`node-pty` not installed~~ → `node-pty ^1.1.0` is in `dependencies`, ConPTY binary built, `main.js` uses it for true PTY with `proc.resize()` wired up.
- ~~`electron-builder` not in devDeps~~ → `electron-builder ^26.8.1` is in `devDependencies`; `npm run build` invokes it cleanly (verified `--version` returns 26.8.1).
- ~~`pref-new-doc` / `pref-backup` stubs~~ → Both pages are fully implemented (`loadNewDocPrefs`, `saveNewDocPrefs`, `loadBackupPrefs`, `saveBackupPrefs`, `runBackup`, `startAutoBackup`). Fixed real bug: the backup "Browse…" button incorrectly treated `openFolderPicker`'s return value as a dialog result object instead of the string path it actually returns.
- ~~Whiteboard text tool toolbar interaction~~ → Fixed three issues in `whiteboard.js`:
  1. Only `#text-opts` had `mousedown preventDefault`; clicking the colour palette (always visible) blurred the active text-edit and committed it prematurely. Broadened the handler to the whole `#toolbar` so no toolbar click steals focus while `editId` is set.
  2. Size/Bold/Italic click handlers ran the `selIds.forEach` block in addition to the `editId` block — since `selIds` contains `editId` for a new text, this duplicated work and called `pushHist()` on still-empty text. Each toolbar click became a phantom undo step. Now: while editing, only the editing-element/tinp branch runs; `selIds` branch is only used when no edit is active.
  3. Colour swatch clicks during a text edit called `applyClrToSel()` → `pushHist()` mid-edit. Now: while editing, the swatch live-updates `tinp.style.color` and `el.sc` without pushing history (the final snapshot lands at `commitText`).

## Whiteboard Architecture (Excalidraw, Session 7+)

### Component layout
- The whiteboard tab renders an iframe pointing at `src/whiteboard.html`.
- `whiteboard.html` loads `whiteboard.bundle.js` (an esbuild-bundled React app, source: `src/whiteboard-app.jsx`) which mounts `<Excalidraw />` full-viewport.
- Excalidraw's fonts are self-hosted: `build-whiteboard.js` copies `node_modules/@excalidraw/excalidraw/dist/prod/fonts/` into `src/excalidraw-fonts/`, and the bundle sets `window.EXCALIDRAW_ASSET_PATH = './excalidraw-fonts/'` so no CDN call ever happens.
- React / Excalidraw run **only inside the iframe**. The main renderer (`renderer.js`) remains pure JS — no React, no JSX, no bundler.

### Build pipeline
- `npm run build:wb` invokes `build-whiteboard.js`:
  1. Copies fonts → `src/excalidraw-fonts/`
  2. esbuild bundles `whiteboard-app.jsx` → `src/whiteboard.bundle.{js,css}` (IIFE, target Chrome 120, minified, `production` export condition)
  3. Asset files (woff2, etc.) emitted to `src/whiteboard-assets/`
- `npm start` runs `build:wb` then `electron .`
- `npm install` triggers `build:wb` via `postinstall`
- All build artefacts under `src/whiteboard.bundle.*`, `src/whiteboard-assets/`, `src/excalidraw-fonts/` are gitignored

### File Format
Whiteboard files are JSON. Two versions coexist:

**v2 (current — written for all new whiteboards):**
```json
{ "__wb__": true, "version": 2, "source": "excalidraw",
  "elements": [/* Excalidraw scene elements */],
  "appState": { /* slim subset — viewBackgroundColor, scroll, zoom, etc. */ },
  "files":    { /* Excalidraw binary files (images) */ } }
```

**v1 (legacy — still readable, auto-migrated on first edit):**
```json
{ "__wb__": true, "version": 1,
  "elements": [/* old custom-canvas shapes */],
  "idCounter": 5, "camera": { "x": 0, "y": 0, "zoom": 1 } }
```

Detection in `openFile()`: a `.json` / `.whiteboard` / `.excalidraw` file routes to a whiteboard tab if the parsed JSON has `__wb__ === true` OR `type === "excalidraw"` (raw Excalidraw exports). Conversion v1 → v2 happens inside the iframe (`legacyV1ToV2()` in `whiteboard-app.jsx`) when the file is loaded; the next user edit causes the v2 envelope to be written back to disk.

### postMessage Protocol (parent renderer ↔ whiteboard iframe)
_Unchanged from v1 — the iframe still speaks the same wire protocol; only the payload schema inside `content` changed._

| Direction | Message type | Payload |
|---|---|---|
| renderer → wb | `wb-load` | `{ content: string }` — v1 or v2 JSON envelope |
| renderer → wb | `wb-theme` | `{ dark: bool }` |
| renderer → wb | `wb-get-data` | (no payload) — requests current state |
| wb → renderer | `wb-ready` | (no payload) — React mounted + api available |
| wb → renderer | `wb-state` | `{ content: string }` — v2 envelope, debounced 350 ms after each Excalidraw `onChange` |
| wb → renderer | `wb-data` | `{ content: string }` — response to `wb-get-data` |
| wb → renderer | `wb-save-request` | (no payload) — Ctrl+S inside iframe |

### Auto-save Flow
1. User edits in Excalidraw → `onChange(elements, appState, files)` fires
2. `whiteboard-app.jsx` debounces 350 ms → builds v2 envelope → `wb-state` postMessage
3. Dedup guard (`lastSerialized.current`) suppresses no-op states caused by `updateScene` echoes
4. Renderer `wb-state` handler → `tab.content = m.content`, `tab.dirty = true`
5. `scheduleAutoSave()` → 1.5 s → `saveSession()` (session JSON in AppData)
6. `scheduleWbFileSave(tab)` → 1.5 s → `writeFile(tab.filePath, tab.content)` → `tab.dirty = false`

### Storage Paths
- Session (all tabs): `%AppData%\notepp\autosave\session.json`
- New whiteboard backing files: `%AppData%\notepp\Whiteboards\whiteboard-N.json` (v2 envelope)
- User-chosen save path: wherever the user picks via Save As dialog (`.json`, `.excalidraw`, or legacy `.whiteboard`)
